import { TestBed } from '@angular/core/testing';

describe('newsResolverResolver', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({});
  });

  it('should be created', () => {
    
  });
});
